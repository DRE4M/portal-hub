server {
    listen 80;
    server_name localhost;

    # 메인 포털 대문 (portal-hub)
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
